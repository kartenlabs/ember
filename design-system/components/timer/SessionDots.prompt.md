Where you are in the current set of pomodoros.

```jsx
<SessionDots label="Set" total={4} completed={2} current={2} />
```

- Squares, not dots, despite the name — nothing in Ember is round.
- The running cell breathes on a 4s cycle; completed cells fill amber; the trailing clay cell is the long break.
