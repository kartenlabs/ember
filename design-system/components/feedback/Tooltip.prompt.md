Names an icon-only control on hover. Inverted cream chip, mono text.

```jsx
<Tooltip label="Reset timer" shortcut="R"><IconButton icon="reload" label="Reset" /></Tooltip>
```

- Only ever a label (plus an optional shortcut). Never a paragraph, never interactive content.
- If the information is required, put it on screen instead.
